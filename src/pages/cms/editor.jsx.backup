import React, { useEffect, useState } from 'react'
import { requireAuth, getUser, logout } from '@/middleware/auth'
import Link from 'next/link'

export default function CMSEditor() {
  const [user, setUser] = useState(null)

  useEffect(() => {
    requireAuth()
    const userData = getUser()
    if (userData && (userData.role === 'admin' || userData.role === 'masteracct')) {
      setUser(userData)
    } else {
      window.location.href = '/crm/dashboard'
    }
  }, [])

  if (!user) return null

  return (
    <div className="min-h-screen bg-slate-50">
      {/* Header */}
      <header className="bg-navy-900 text-white shadow-lg">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div className="flex justify-between items-center">
            <h1 className="text-2xl font-bold">Kashflow Kathy CMS</h1>
            <div className="flex items-center gap-4">
              <Link href="/crm/dashboard" className="text-gold-400 hover:text-gold-300">
                Back to CRM
              </Link>
              <span className="text-gold-400">Welcome, {user.username}</span>
              <button 
                onClick={logout}
                className="px-4 py-2 bg-red-600 hover:bg-red-700 rounded-lg transition-colors"
              >
                Logout
              </button>
            </div>
          </div>
        </div>
      </header>

      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div className="bg-white rounded-xl shadow-lg p-8 mb-8">
          <h2 className="text-2xl font-bold text-navy-900 mb-6">Content Management System</h2>
          
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {/* Pages */}
            <Link href="/cms/pages" className="border-2 border-navy-200 rounded-xl p-6 hover:border-gold-400 transition-colors">
              <div className="text-4xl mb-4">📄</div>
              <h3 className="text-xl font-bold text-navy-900 mb-2">Pages</h3>
              <p className="text-slate-600 mb-4">Edit homepage, about, services, and other pages</p>
              <div className="w-full px-4 py-2 bg-navy-900 text-white rounded-lg text-center">
                Manage Pages
              </div>
            </Link>

            {/* Media */}
            <div className="border-2 border-navy-200 rounded-xl p-6 hover:border-gold-400 transition-colors">
              <div className="text-4xl mb-4">🖼️</div>
              <h3 className="text-xl font-bold text-navy-900 mb-2">Media Library</h3>
              <p className="text-slate-600 mb-4">Upload and manage images, videos, and documents</p>
              <div className="w-full px-4 py-2 bg-navy-900 text-white rounded-lg text-center">
                Media Library
              </div>
            </div>

            {/* Settings */}
            <Link href="/cms/settings" className="border-2 border-navy-200 rounded-xl p-6 hover:border-gold-400 transition-colors">
              <div className="text-4xl mb-4">⚙️</div>
              <h3 className="text-xl font-bold text-navy-900 mb-2">Settings</h3>
              <p className="text-slate-600 mb-4">Configure site settings, SEO, and integrations</p>
              <div className="w-full px-4 py-2 bg-navy-900 text-white rounded-lg text-center">
                Site Settings
              </div>
            </Link>
          </div>

          {/* Profile Section with Kathy's Image */}
          <div className="mt-8 border-t pt-8">
            <h3 className="text-xl font-bold text-navy-900 mb-4">Profile Images</h3>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div className="border rounded-lg p-4">
                <p className="text-sm text-slate-600 mb-2">Circular Profile (Homepage)</p>
                <div className="w-48 h-48 mx-auto rounded-full overflow-hidden border-4 border-gold-400">
                  <img 
                    src="/images/kathy-ferguson-circular.png" 
                    alt="Kathy Ferguson Circular"
                    className="w-full h-full object-cover"
                  />
                </div>
              </div>
              <div className="border rounded-lg p-4">
                <p className="text-sm text-slate-600 mb-2">Standard Profile (About Page)</p>
                <div className="w-48 h-48 mx-auto rounded-lg overflow-hidden">
                  <img 
                    src="/images/kathy-ferguson.png" 
                    alt="Kathy Ferguson"
                    className="w-full h-full object-cover"
                  />
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  )
}
